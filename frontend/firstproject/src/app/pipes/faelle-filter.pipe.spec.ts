import { FaelleFilterPipe } from './faelle-filter.pipe';

describe('FaelleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FaelleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
